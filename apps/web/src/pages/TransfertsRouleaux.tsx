import { TransfertsScreen } from './transferts/TransfertsScreen'

// Transferts › Rouleaux — bons de transfert of écru (tombé de métier) + fini
// rolls (legacy "Transferts pièces").
export function TransfertsRouleaux() {
  return <TransfertsScreen kind="rouleaux" />
}

# Screen notes — per-screen rules and post-mortems

> Moved verbatim from `CLAUDE.md` § Navigation Structure on 2026-09-07. One section per menu, in menu order. `CLAUDE.md` keeps one line per menu; the detailed rules (ticket post-mortems, measured data facts, guard scripts) live here. Add new per-screen notes HERE. Canonical reference screens and their patterns are in `implemented_screens.md`.

## Tickets widget (header, LIVA issue tracker)

LIVA issue tracker via server-side proxy `apps/api/src/routes/tickets.ts` — one router factory mounted twice, factures.ts-style: `/api/tickets/*` (product `etm-erp`, this app) and `/api/tickets-trm/*` (product `trm-erp`, the sister TRM app's widget). Env `ISSUE_TRACKER_URL/_API_KEY/_PRODUCT_SLUG/_PRODUCT_SLUG_TRM` — required in prod env too; widget `apps/web/src/components/tickets/` + header trigger, screenshot via lazy `html-to-image`. **Widget feature version 1.3.0** (spec + upgrade path: `issue_tracker_integration` skill) — "Mes tickets" collapses closed tickets into a drawer, and the header trigger carries a red unread badge computed from `localStorage` signatures per user (`useTicketNotifications.ts`, **no DB change**). Two scoping rules: `resolu` counts as **open** (fixed but its release hasn't shipped — the tracker flips it to `ferme` on publish, and only then does it belong in the drawer), and every read is pinned to `product_slug` because the tracker API key is *company*-scoped (without it a reporter using the same email in MFProd sees MFProd tickets here). v1.2.0 adds the **follow-up opt-in**: a checkbox on the form (unticked by default) and a §35 toggle in the ticket detail set the tracker’s `bugs.follow_up`, and the tracker then emails the reporter on every **status change** of that ticket — proxy route `PATCH /:id/follow` (owner-checked like detail/attachments), guard script `src/scripts/check-tickets-follow.ts`. Needs a tracker at least at the `follow_up` migration, or the toggle 404s. v1.3.0: **an account without a mapped email still reports** — the proxy files it under a stable synthetic identity `utilisateur-<id>@mps.malterre.invalid` (`lib/tickets-reporter.ts`; the tracker keys reporters by email but only *sends* to it for follow-up), forces `follow_up` off, refuses `PATCH /:id/follow`, and `GET /reporter` → `{ name, can_follow }` lets the widget hide the follow-up controls. Until then those accounts got a 400 — that was the visitage compte-poste and Mickael Grivelet. A shared station may send `reporter_name` (who is at the keyboard), honoured only for a synthetic reporter and appended to the account name. ⚠️ Mapping an email later moves the account to a new tracker identity (earlier tickets leave « Mes tickets ») — accepted


## 2. Prospects

**Prospects** (renamed from legacy "Marketing") — **Demandes** (`/prospects/demandes`; catalogue requests from the `prospect` table; master-detail, implemented)

    **Prospects › Demandes — devis prospect** (LIVA #1112, ports legacy `FEN_devis_prospect.wdw`): « Créer un devis » (header button + Devis sidebar tab) creates a `devis_etm` row with **`IDprospect > 0` and `IDclient = 0`**, prefilled from the demande, and jumps to **Clients › Devis** (`?devis=<id>` deep link) — the devis editor is NOT forked; the shared screen serves both populations with a violet « Prospect » chip. Legacy conventions kept for WinDev compatibility: both address FKs pinned to **adresse 795 « A Définir »** (the detail/PDF/email override the display with the prospect's own fields), `IDmode_paiement = 3`, `IDecheance = 11`, expiration = date + 1 mois, and the **numero sequence is shared** with client devis. A prospect devis can NEVER become a commande (`/convert` 400s) — **converting the prospect to a client re-homes its OPEN devis** (`IDclient` set, `IDprospect` cleared, addresses moved onto the new client's default adresse), after which they transform normally; soldé devis stay on the prospect as history. **Two permission keys, category « Prospects », both default-closed (no seed — user decision 2026-09-02)**: `edit_prospects` gates every write of `routes/prospects.ts` (create/edit/delete/statut/convert — the router was fully ungated before), `devis_prospect` gates prospect-devis writes in `routes/devis.ts` (create/edit/lignes/état/delete/email/documents; client-devis writes stay ungated as before, and reads + PDF stay open to all). Guard: `check-devis-prospect.ts` (`--roundtrip`) — pins the 17 legacy rows, the 795 placeholder, and the PDF element-tree carrying the prospect's name/address.
## 3. Clients

**Clients** — Commandes, Devis, Facturation, Gestion

    **Clients › Gestion — compte client** (`client.compte`, the chart-of-accounts code): mandatory and format-checked. "+ Nouveau" opens a **création dialog** (nom / secteur / activité) rather than inserting a `Nouveau client` placeholder — knowing the name before the INSERT is what lets the code be derived from it. Generation lives in `apps/api/src/lib/compte-client.ts` (`pickCompte`), mirrored client-side by `apps/web/src/lib/compte-client.ts` for live validation; guard `apps/api/src/scripts/check-compte-client.ts` replays it over all 654 clients. **Generation emits `411` + 3 alphanumerics, validation accepts `411` + 3-or-more** — the legacy table holds 388 four-char codes that must stay editable (225 are three-char, 36 are other shapes). Mnemonic candidates come from the name's significant words, skipping legal forms (SARL/SAS/SOCIETE…) and industry-generic words (BONNETERIE/TRANSPORTS/ATELIER…) exactly as the accountant did — "Bonneterie Gautier" → `411GAU`; collisions fall through consonant squeezes then numeric variants (`411SO2`), with an exhaustive base-36 sweep as the guaranteed last resort. Uniqueness is **not** scoped by `IDsociete` (the code identifies the company in the ledger) and is enforced **only on values that change** — 10 duplicate codes already exist in legacy data and must stay saveable. The duplicate check runs **live as the user types**, on both the dialog and the Info tab: `GET /clients/comptes` returns every code in use and `useComptesPris()` caches it under one query key (React Query dedupes, so the InfoTab call costs nothing extra), which greys out Créer / Enregistrer instead of ambushing the user after the click. Server-side re-validation on POST/PUT stays the authority — the cached set can go stale if someone else creates a client meanwhile. Entering edit mode on a client whose compte is empty or malformed auto-fills a suggestion (snapshot updated too, so the pre-fill alone isn't "dirty"); an invalid value blocks the save and the exit via `useUnsavedGuard`'s `shouldBlockExit` (`mps_designer §28.5b`). Only the `edit_client_info` scope writes the column, so validation is gated on that permission — a user who cannot edit it is never blocked by it.
    **Clients › Gestion — SIREN** (`client.siren`, ticket #1088): 9 chiffres, dans la carte Facturation sous le N° TVA. **Facultatif** — la colonne se remplit client par client, donc seule une valeur non vide est contrôlée (sinon 400 `siren_invalide` + exit bloqué via le même `infoIssue` que le compte). ⚠️ **La clé de Luhn est un AVERTISSEMENT, jamais un refus** : le registre a porté des identifiants qui ne la vérifient pas, et bloquer une saisie légitime coûte plus cher que la faute de frappe qu'on attrape. **Colonne détenue par la fiche ETM seule** — `clients-trm.ts` ne la nomme pas (invariant ci-dessous), l'ajouter côté TRM demandera de la nommer là-bas. **Elle s'imprime sur la facture, l'avoir et le proforma** (LIVA #1130, 2026-09-07) : ligne « SIREN · 123 456 789 » en tête de la carte des conditions, avant le N° TVA, **lue sur `client` au moment du rendu et jamais recopiée sur `facture`** — un SIREN identifie l'entité pour toute sa vie, un autre SIREN serait un autre client, donc il n'y a pas de valeur « à l'émission » à figer comme pour le `num_tva` ; un client sans SIREN n'a simplement pas la ligne. Les deux bâtisseurs (`buildFacturePdfData` dans `factures.ts`, les deux montages ETM/TRM, et `buildProformaPdfData` dans `commandes-client.ts`) passent par `formatSirenForDocument` / `loadClientSirenForDocument` de `lib/siren.ts`. La facturation électronique proprement dite reste à faire. Helpers `lib/siren.ts` (API + miroir web), garde `check-siren.ts` (`--roundtrip`, + section arbre d'éléments du PDF).
    **TVA — the rate belongs to the CLIENT, never to the société.** Every customer-facing document takes it from `client.IDtva` → `tva.valeur` via `apps/api/src/lib/tva.ts` (`loadClientTvaRate`, falling back to the société-1 `est_defaut` row **only** when the client has no row). Each société has an "Exonération" row at `valeur = 0` used for export customers (Maroc, hors UE): a 0 rate is a real value, not "unset", so it must never fall through to the default. A 0 rate also **collapses the totals block** — no TVA line, no TTC line, the document ends on `TOTAL HT` (the sub-total row survives only when a remise or frais de port makes it differ). Applies to the confirmation de commande, the devis, the proforma and the facture; a `facture` additionally **stores its own `IDtva`** copied from the client at creation, so an old invoice keeps the rate it was issued with. Guard: `check-tva-exoneration.ts`.
    **Clients › Commandes — proforma : mention d'ajustement sous les totaux** (LIVA #1164, 2026-09-17). La facture proforma (`GET /commandes-client/:id/proforma/pdf`, `buildProformaPdfData`) imprime, juste sous la ligne TOTAL TTC et dans la même colonne, « Le montant définitif sera ajusté à la livraison selon les métrages réellement produits et livrés. » (`PROFORMA_NOTE` dans `FacturePdf.tsx`, gardée par `isProforma` — jamais sur la facture ni l'avoir). Le bloc totaux + mention est un seul `wrap={false}` pour que la phrase ne parte jamais seule en page suivante. Test arbre d'éléments : `FacturePdf.test.ts`.
    **Clients › Gestion — the `client` table has TWO ledgers.** `client` is partitioned by `IDsociete` (1 ETM / 2 Tricotage Malterre / 3 Confection), so this API serves two fiches: `routes/clients.ts` (`/api/clients`, ETM) and `routes/clients-trm.ts` (`/api/clients-trm`, consumed by the TRM app — its screen lives in the sibling repo and is deliberately NOT an `@etm` shared screen: the two fiches show different fields). Shared plumbing lives in **`lib/clients-common.ts`** — SQL/format helpers, `requirePermission`, `repairNames`, `countClientActivity`, the accented-flag helpers `setClientFlag`/`readClientFlag` (`archivé` + `bloqué`, delete + positional reinsert on Linux), and `registerContactAdresseRoutes()` (`contact`/`adresse` are polymorphic on `IDclient` and NOT partitioned, so both mounts register them verbatim). **The invariant that keeps the two fiches from wrecking each other: a router must never NAME a column the other fiche owns** — an unnamed column keeps its stored value on `UPDATE`, a named one is zeroed on every save. ETM owns `client_interne` / `IDsecteur_activite` / `IDactivite` / `journal_commercial` / `dernier_contact` / `inclureRapportQualite` / `pct_ajeol`; TRM owns `rib` / `domiciliation` / `IDtransporteur` / `bloqué` (its « Attente paiement facture » checkbox). **`tva` and `code_comptable` are partitioned too** — serving ETM's rows to a TRM screen would silently rewrite a client's VAT to another company's row on the next save. Full detail: `claude_doc/implemented_screens.md` § Clients TRM.
    **Clients › Commandes — le prix d'une ligne écru/fini dépend du CLIENT, jamais du seul catalogue.** `lib/tarif-client.ts` (extrait de `routes/clients.ts`, partagé avec la fiche client) résout le mode tarifaire de la paire (référence × coloris) via `designation_client` → `ref_client_colori`, et `calcLignePriceClient` en tient compte : **contrat actif** → le `prix_saisi` négocié (€/Ml, converti en €/Kg par le rendement pour une ligne Kg) et un nudge Tricobot restreint aux tranches que ce contrat cote ; **coefficient fixe** → la marge du client à la place de la dégressive ; **standard** sinon. ⚠️ **Un contrat expiré ne retombe PAS sur la grille standard** — la référence n'est plus vendable tant qu'un nouveau contrat n'est pas établi (même règle que la fiche client) : l'endpoint renvoie `blocked` + `blocked_reason` dès le choix du coloris, le dialogue affiche le bandeau rouge et grise Enregistrer, et POST/PUT `/lignes` répondent **409 `contrat_expire`**. Le PUT ne bloque que sur un changement **commercial** (référence, coloris, quantité, prix) : une ligne prise quand le contrat était valide reste modifiable pour sa date et son commentaire. Bug d'origine (30/07/2026) : l'auto-pricer ne recevait pas le client, donc une commande C2TEC est passée un mois après l'expiration du contrat #475, sans rien à l'écran — et un client sous contrat actif était coté 12,00 €/Ml au lieu de 3,81 €/Ml négociés. Garde : `apps/api/src/scripts/check-contrat-tarif-ligne.ts` (épingle le cas C2TEC + rejoue tous les contrats actifs et coefficients de la base).

    **Clients › Gestion / Commandes — un contrat sur un tombé de métier est en €/Kg, jamais converti par le rendement** (LIVA #1144, 2026-09-10). Le tombé de métier se vend **toujours au Kg** (Vincent) ; la règle « `prix_saisi` négocié en €/Ml, converti en €/Kg par le rendement » de #1068 venait d'un cas fini (C2TEC) et s'appliquait aussi aux lignes écru : le contrat 479 de Gant Maille sur la réf 254 (1 rouleau à 10,01, désignation en Kg, rendement 2,82) était proposé à 28,23 €/Kg sur la commande n° 3869, qu'Isabelle a corrigée à la main. Mesure sur la base : 8 contrats écru en Kg, 8 fois les lignes au `prix_saisi` tel quel ; 165 contrats fini sur 192 aux lignes au `prix_saisi` en Ml. Correctif : `contratPriceFn` prend le `kind` de la référence (`'ecru'` → €/Kg, `'fini'` → €/Ml, l'autre unité convertie par le rendement) ; le chemin écru du pricer passe par le nouveau `calcTarifRefEcru` (`lib/pricing-fini-tarif.ts`, réduction nType_Ref = 1 : fil + tricotage, `qte_kg` sur chaque tranche, `kind` sur le résultat). **Deuxième défaut du ticket : la fiche client refusait l'onglet Tarifs et le clic coloris dès que `IDref_fini = 0`** (« réservé aux références ennoblies ») alors que 9 contrats écru existent en base — Isabelle ne pouvait ni voir ni renouveler le contrat. `GET /clients/:id/coloris/:rccId/tarif` répond désormais pour une désignation tombé de métier via `calcTarifRefEcru` et porte `prix_unit` (`'Kg'`/`'Ml'`) ; `TarifDialog` affiche Qté/Prix dans cette unité (pas de section Traitement, ligne Ml seulement si rendement) et `TarifModeDialog` libelle les prix négociés en €/Kg (`prixUnit` sur la cible, `prixUnitOf(ref)`). Le sélecteur d'impression des tarifs (`TarifsSelectionDialog`) reste fini seulement — le PDF n'a pas de rendu écru. Garde : `check-contrat-tarif-ligne.ts` §4bis (Gant Maille : 320 kg → 10,01 €/Kg) et le balayage §5 rejoue aussi les paires écru en Kg ; sondes en lecture seule `probe-1144-gant-maille.ts` (ligne, contrat, commandes — se lance aussi sur l'hôte API prod avec `--env-file=.env`) et `probe-1144-contrat-units.ts` (unité des `prix_saisi` vs lignes). ⚠️ En base locale (laptop), la colonne `client.siren` manquait (ajoutée le 2026-09-10 par `ALTER TABLE client ADD siren VARCHAR(20)`, APIs arrêtées d'abord : « fichier en cours d'utilisation ») — sans elle `GET /clients/:id` répond 500 et la fiche reste vide.

    **Clients › Commandes — le sélecteur de coloris est limité au catalogue du client.** `/lookups/colori-ecru|colori-fini` acceptent `client` (+ `current`) et ne proposent alors que les coloris enregistrés en Gestion pour cette (client × référence) via `ref_client_colori` — une référence teinte pour des dizaines de clients offrait sinon **389 coloris là où le client en achète 71**. Trois états, et les deux derniers **retombent sur la liste complète** (jamais de sélecteur vide, qui rendrait la référence non commandable), chaque ligne portant alors `hors_catalogue: true` pour que l'UI le dise : catalogue exploitable → liste restreinte ; aucun `ref_client_colori` (13 des 2 025 entrées fini actives) → repli ; **catalogue disjoint de la liste affichée** → repli aussi (cas réel client 202 / ref 1426 : les rcc pointent des `colori_ecru` d'un `ref_ecru` que le fini ne référence plus). `current` (le coloris d'une ligne existante) est toujours conservé et marqué — **381 des 7 062 lignes historiques sont hors catalogue** et perdraient sinon leur coloris à l'ouverture du dialogue. ⚠️ **Sans `client` la liste reste complète** : c'est ce dont Clients › Gestion a besoin, puisque c'est l'écran où le catalogue se construit. Garde : `apps/api/src/scripts/check-coloris-client-scope.ts`. Le même angle mort existe encore sur **Devis** (`/devis/lookups/colori-*`).

    **Clients › Gestion — une ligne « ref + coloris » par coloris est l'ancien modèle, archivé, et elle ne doit pas s'afficher** (LIVA #1177, Isabelle, 2026-09-18). Sur Simone Perele, l'onglet Références listait 19 entrées : la LF043 standard (39 coloris dans `ref_client_colori`, la seule commandable), 138A, et **17 « LF043 - 011 BLANC », « LF043 - 316 FUSHIA »… à un seul coloris** — des `designation_client` d'avant le catalogue par coloris, toutes `archivé = 1`, que le legacy cache. L'API les filtrait bien… avec `pick(r, 'archivé', 'archiv')`, une liste de clés EXACTES : sur le bridge Linux la clé revient tronquée à l'accent **plus un octet parasite** (`archiv?`), rien ne correspond, et `archivé` vaut 0 pour toute ligne — le footgun #1090 (`lib/accented-keys.ts`), invisible sous Windows. Dégâts au-delà de l'écran : le sélecteur de coloris de Clients › Commandes (`lib/tarif-client.ts`, même `pick`) proposait les coloris des désignations archivées, et **`client.archivé` lisait 0 aussi** — la pilule « Archivé » de Clients › Gestion était vide en prod (236 clients archivés). Correctif : `pick()` (`lib/clients-common.ts`) tente les clés exactes puis un **préfixe insensible à la casse**, test `clients-common.test.ts` ; la même faute `r.archiv ?? r['archivé']` dans la recherche de désignation du « Soumission Lot Client » (`commandes-sous-traitant.ts`) passe par `pickVal(r, /^archiv/i)`, et la garde a révélé trois autres `row.archivé ?? row.archiv` (`stock-ecru.ts`, `stock-fini.ts`, `etudes-coloris.ts` — plus `invalidé` sur `envoi_email`) : sur la même base, ancien code → nouveau code, le sélecteur de références de Tombé Métier › Stock passait de 398 à 327 écrus, celui de Finis › Stock et d'Études coloris de 610 à 471 finis, le sélecteur de clients d'Études coloris de 667 à 431 — tous listaient les archivés en prod. **Garde statique** : `apps/api/src/scripts/check-accented-key-fallback.ts` échoue sur tout `?? r['…é']` / `?? r.…é` / lecture nue d'une racine tronquée (`r.archiv`). Les deux gardes `check-coloris-client-scope.ts` et `check-contrat-tarif-ligne.ts` ont chacune un ÉCHEC préexistant sur master (volume 394 ≠ pin, date d'expiration 20260731 ≠ pin — dérive des données de dev, pas du code).

    **Clients › Commandes — lancer une commande ennoblisseur depuis la ligne : le lien à la commande client se choisit ROULEAU PAR ROULEAU** (LIVA #1115, port de `FEN_Ennoblir` : sa colonne « Lien » `lien_fermé.png` / `lien_cassé.png` et sa paire Tous / Aucun). Dans le dialogue « Nouvelle commande — <ennoblisseur> », chaque rouleau sélectionné porte une icône lien, **liée par défaut** (comme le `BTN_Suivant` legacy), et une bande « Réservés à la commande de <client> » compte `n / m` avec « Lier tout · Aucun » ; la jauge du pied ne compte que les rouleaux liés. Cas d'usage : « on me commande 1 rouleau, j'en lance 3 pour avoir du stock » — 147 lignes ennoblisseur de la base sont ainsi mixtes (liées + libres). Côté API, `POST …/supply/ennoblissement/orders` prend `linkStockEcruIds` (sous-ensemble de `stockEcruIds`, omis = tout lier, l'ancien comportement) ; un rouleau non lié reste affecté à la ligne teinture avec `IDligne_commande_client = 0`, apparaît en « disponible » sur l'onglet Ennoblissement et se lie plus tard par « Affecter le stock ». Un rouleau `reserved_elsewhere` n'a pas d'icône : il garde sa réservation quoi qu'il arrive. L'en-tête `commande_sous_traitant` reçoit désormais `IDcommande_client` / `IDligne_commande_client` comme le legacy (3 830 des 4 812 commandes ennoblisseur les portent ; rien dans ETM ne les lit, l'écran WinDev si). Mesures : `scripts/probe-1115-lien.ts`.
    **Clients › Commandes — lignes divers** (`ligne_commande_client.TYPE = 3`) price off the **`tarif_divers` grid**, never the roll-tranche tariff of écru/fini: the shared resolver `GET /expeditions/divers/lookups/prix?ref&v1&v2` (exact combo → `(v1,0)` → `(0,0)` → `ref_divers.prix_unitaire`), the same one the Expéditions article dialog uses. Two data facts drive the UX: a **two-axis reference has no `(0,0)` row** ("Tissu Voltige" = 533 cells keyed couleur × taille), so the price is legitimately undefined until *both* axes are picked — an empty field mid-form is not a bug; and ~417 of 1 836 existing divers lines point at refs with **no grid entry at all**, so manual entry must stay open — this field carries **no padlock and no `deverrouiller_tarifs` gate**, unlike écru/fini. A saved line keeps its own `prix` as an order-time snapshot (replaying the grid over legacy lines reproduces 1 346 exactly and differs on 59 that pre-date a price change) — never re-derive an existing line's price; only a stored `0` gets refilled.
    **Clients › Commandes — le contenu d'un carton se saisit en TABLEAU, jamais dans un sélecteur** (modale « Expédition groupée », tickets #1098/#1099). Bouton « Modifier le contenu » → mode édition listant **tous** les articles divers de la commande avec une quantité éditable, filtre + « Remplir » ; enregistrement en un appel `PUT /expeditions/divers/lignes/:id/contenu`. Reproduit le legacy `FEN_Expedition_Groupé` (`TABLE_RefExpedie` = les lignes divers de la commande LEFT JOIN le `ref_divers_expedie` du carton sélectionné). ⚠️ **La clé est le COMBO (ref × variation1 × variation2), pas `IDref_divers_expedie`** — c'est la ligne que l'utilisateur lit et celle que la facture regroupe. Trois faits mesurés le commandent : le tableau doit **unir** les lignes de commande **et** les articles déjà dans le carton (61 des 1 589 articles de cartons rattachés à une commande ne correspondent à aucune ligne — invisibles sinon, donc incorrigeables) ; 7 commandes portent le même combo sur deux lignes (quantités **sommées**, jamais affichées deux fois) ; un total inchangé **n'écrit rien**, ce qui protège l'unique carton qui détient deux lignes pour un même combo. ⚠️ **Ne jamais proposer de remplir depuis « Cmdé »** : 1 443 des 1 770 combos (81,5 %) sont déjà expédiés en tout ou partie, donc c'est **« Reste »** (= commandé − expédié ailleurs) qui remplit la ligne ; Cmdé reste un repère, non cliquable. ⚠️ **`stock_divers` est décrémenté à l'écriture**, donc le stock stocké exclut DÉJÀ le contenu courant du carton : la référence de comparaison est `stock + quantité déjà dans ce carton`, sinon un réenregistrement à l'identique signale un dépassement de stock. Stock et Reste s'affichent **après enregistrement** (ils décomptent pendant la saisie) et restent des **avertissements, jamais des blocages** — 584 des combos de lignes n'ont aucune ligne `stock_divers`. Garde : `check-divers-carton-contenu.ts` (`--roundtrip`).
    **Clients › Expéditions — l'avis d'expédition divers ne porte AUCUN prix** (LIVA #1127, Isabelle, 2026-09-07). C'est un document de livraison : le legacy `ETAT_Expédition_diverse` (modèle DIV632, Unicycle) imprime désignation + « N Pièce » et rien d'autre, et le client ne doit pas y lire un prix — l'argent est sur la facture. Jusqu'à #1127 `BonLivraisonDiversPdf.tsx` ajoutait les colonnes P.U. / TOTAL (€) et un total général en euros dès qu'un article avait un prix, donc sur toute expédition Unicycle facturable. Le type `BlDiversItem` n'a plus de champ `prix` du tout (un prix ne peut pas revenir par le builder sans erreur de compilation) ; la ligne de pied de chaque carton et la case dorée comptent désormais les **pièces par unité** (`quantiteParUnite`, « 13 unités · 2,50 Ml »), le chiffre que le client pointe contre le colis. Garde sans base : `check-bl-divers-sans-prix.ts` (`[id]` pour épingler aussi un document réel — 599 sur la copie dev en portait). Reste ouvert : l'unité 4 s'imprime « unité » quand le modèle legacy dit « Pièce » (`uniteLabel`, partagé avec l'écran).
    **Clients › Expéditions — supprimer un avis (ou en retirer un rouleau) DÉ-EXPÉDIE le rouleau** (suite de #1086, trouvé sur l'AE 9698 Save Futur, 2026-09-08). « Un rouleau est expédié » = deux faits, `stock_fini.IDligne_expedition > 0` ET `IDetat_stock_fini = 4` ; `DELETE /expeditions/formelle/:id` et `DELETE …/rolls/:stockId` ne remettaient à 0 que le premier, laissant un fantôme « Expédié » sans avis que Finis › Stock cache (`<> 4`), que Gestion › Marchandise ne voit plus (INNER JOIN sur la ligne d'avis) et que le sélecteur de rouleaux de la ligne ne repropose jamais. `unshipFiniRolls()` (`expeditions.ts`) remet `IDligne_expedition = 0` ET rétrograde l'état 4 → 3 « Validé » (un autre état est conservé, c'est le classement du magasin) ; ⚠️ **contrairement au retour-stock, `IDligne_commande_client` est GARDÉ** — supprimer un avis annule une expédition, pas la commande pour laquelle le rouleau avait été choisi, donc il revient exactement là où il était avant d'être coché (affecté à sa ligne, proposé de nouveau par le sélecteur). L'écru n'a pas de colonne d'état : son seul fait est `IDligne_expedition_ETM`. Garde avec écriture puis restauration, localhost seulement : `check-expedition-unship.ts` (rouleau seul avec voisin intact, autre état conservé, ligne entière). Contexte du ticket : l'AE 9698 (4 m de 186A recoupés sur 909/15, réexpédiés le 19/11/2024 sur une commande déjà facturée) ressortait à chaque « Générer les factures » ; réglé en la passant en **donation**, le générateur les saute — la suppression aurait produit ce fantôme.
    **Clients › Commandes — attacher une pièce à une DONATION est la sortie de stock, il n’y a jamais d’avis derrière** (LIVA #1154, Isabelle, 2026-09-11 : commande 3868, pièce 3041/4 toujours dans Finis › Stock). Mesuré en prod : 877 des 888 rouleaux finis de donation sont en état 4 « Expédié » sans `IDligne_expedition`, aucune commande donation n’a d’`expedition` ; les 10 restants en état 3 étaient exactement les deux commandes saisies dans ETM (3840 du 23/07 et 3868). Le legacy `FEN_Ligne_Donation` tamponnait l’état 4 à la validation (son propre picker offre `IDetat_stock_fini = 3 OR IDcommande_donation = <cette commande>`), le picker NG n’écrivait que la FK. Règle (`lib/donation-pieces.ts`, tests `donation-pieces.test.ts`) : **fini** → attacher = `IDcommande_donation` + état 4 dans le même UPDATE ; détacher (décocher, ou `DELETE` de la commande) = état 4 → 3 **seulement sur les rouleaux encore en 4** puis FK à 0, dans cet ordre (même règle qu’`unshipFiniRolls`, sinon fantôme « Expédié » sans avis). **Écru** → la FK seule est le fait de sortie (pas de colonne d’état) : Tombé Métier › Stock l’exclut désormais sur TOUS les onglets comme le legacy `FI_Stock_TM_ETM` (`AND stock_ecru.IDcommande_donation = 0`), plus seulement sous « Disponible ». Les candidats du picker restent lisibles après coup via la branche `IDcommande_donation = id`. Garde + réparation : `check-donation-etat.ts [--repair]` (les 10 rouleaux réparés en prod le 11/09/2026). Le PDF « Calcul de la valeur » lit par la FK, indifférent à l’état.

    **Clients › Commandes — une pièce donnée ne reste pas affectée à une commande client ; la jauge « Affecté » exclut les donations** (LIVA #1173, Pierre-Emmanuel, 2026-09-18 : commande 3795 THUASNE, « Affecté 192,6 / 200 Ml » alors que les trois rouleaux finis listés font 162,8). Les 29,8 Ml manquants étaient la pièce écru 1448/17 : 10 kg de 2e choix tricotés en 2022, attachée à la commande de donation permanente 2071 (client « Missing ») ET portant encore `IDligne_commande_client` = la ligne Thuasne ; 10 × rendement 2,97767 = 29,8. Depuis #1154 tout lecteur de stock masque une pièce donnée, l’onglet Affectation ne liste que les finis d’une ligne finie et l’onglet Ennoblissement que l’écru chez un teinturier : personne ne pouvait la voir ni la retirer. **Cause** : `stock_ecru.IDcommande_donation` et `.IDligne_commande_client` sont deux colonnes indépendantes, HFSQL n’impose rien entre elles, et les fenêtres WinDev (`FEN_Ligne_Donation`, affectation) écrivent chacune la sienne sans regarder l’autre ; les pickers ETM refusent les deux sens depuis juillet 2026 (donation → jamais une pièce réservée, affectation → jamais une pièce donnée), et les 50 pièces trouvées en prod (24 écru dont neuf de la 3510 sur la commande 3793, 26 finis) sont toutes sur des commandes antérieures au 23/07/2026. **Règle** (`lib/donation-pieces.ts`, tests `donation-pieces.test.ts`) : (1) attacher à une donation = `IDligne_commande_client = 0` dans le même UPDATE, écru comme fini, détacher ne la restaure jamais ; (2) `lineReservationAggregates()` filtre `IDcommande_donation = 0` sur l’écru ET le fini — c’est la seule protection qui tienne tant que le legacy tourne sur les mêmes fichiers ; (3) les quatre routes d’affectation (`PUT …/pieces/ecru|fini/:id`, `POST …/pieces/:kind/affecter`, lien d’un rouleau d’ennoblissement) répondent **409 `piece_donnee`** via `refuseIfDonated()` — ceinture et bretelles, les pools les cachent déjà. Garde + réparation : `check-donation-lien-client.ts [--repair]` (lancé depuis le laptop avec `HFSQL_CONNECTION_STRING` pointé sur `10.10.20.2`, `NODE_ENV=production` : 50 / 50 réparées en prod le 18/09/2026, la jauge de 3795 lit 162,8 sans redéploiement puisque la donnée est saine). Le rendement ne vient pas de la pièce : l’écru porte `metrage = 0`, la jauge calcule poids × rendement de la ref de la LIGNE.
    **Clients › Facturation — « Générer les factures » balaie DEUX registres d'expédition, avec deux groupements** (`POST /factures/prov/generate`). **Formelle** (`expedition`) : un proforma **par client × adresse de facturation de la commande × adresse de livraison de l'avis** (`lib/facturation-groupes.ts`, LIVA #1117 — deux avis d'un même client partis à deux adresses ne partagent jamais une facture ; le proforma est estampillé de l'adresse de facturation de la **commande**, règle legacy vérifiée sur 3 317 des 3 331 factures, le défaut client ne servant que si la commande n'en porte pas). ⚠️ Le legacy ne découpait PAS par livraison — 207 de ses factures en mêlent plusieurs — et Isabelle n'obtenait deux factures qu'en générant après chaque avis. Le dialogue de résultat nomme l'adresse sous le client quand celui-ci a été réparti. Garde : `check-facturation-adresses.ts` + `facturation-groupes.test.ts` ; bout-en-bout avec rollback : `e2e-formelle-generate.ts`. **Divers** (`expedition_divers`) : un proforma **par expédition** — groupement de legacy, imposé par le schéma, le lien retour étant la colonne d'en-tête `IDexpedition_divers` qui ne tient qu'un id (535 factures legacy, une expédition chacune, jamais mêlée à une ligne formelle). Ce back-pointer est écrit sur `facture_prov` puis recopié à la conversion, et `wipeOpenProformas` s'en sert pour rouvrir l'expédition ; ⚠️ **ce n'est PAS un « marqueur de conversion »** — un ancien commentaire l'affirmait, 0 ligne en base l'utilisait ainsi. Divers = ETM seulement (pas de colonne `IDsociete`) → passe **sautée sur le montage TRM**. Les règles de ligne, reconstituées puis validées sur le registre définitif, sont commentées dans `factures.ts` ; les deux contre-intuitives : **un même article réparti sur plusieurs cartons ne fait qu'UNE ligne** portant le total, et `unite = 4` s'imprime **« Pièce »** sur une facture là où le bon de livraison dit « unité ». Frais de port : **une fois par commande pour tout le run**, garde partagée par les deux passes. Garde : `check-divers-facturation.ts` (rejoue les 496 expéditions facturées ; échoue sur toute divergence au-dessus de l'avis 575 — en dessous les factures ont été saisies à la main). Bout-en-bout avec rollback : `e2e-divers-generate.ts`.

    **Clients › Facturation — première page blanche sur la facture 9228** (LIVA #1148, 2026-09-11) : la table des lignes tenait sur la page 1 à 3,5 pt près, mais son `marginBottom: 16` dépassait la réserve et le paginateur de react-pdf déplace alors le bloc ENTIER (jamais une coupe entre lignes) → page 1 vide sous les cartes, totaux page 2, RIB page 3. Correctif : plus de marge basse sur la table, l'écart passe en `marginTop` du bloc des totaux, sur les cinq documents à table + totaux ; test `FacturePdf.test.ts` (forme exacte de la 9228 → 2 pages). Le pourquoi et la méthode de diagnostic : `claude_doc/pdf_email.md` § Pagination.

    **Clients › Facturation — « Marquer comme envoyée » (LIVA #1174, 2026-09-18).** Le cadre rouge « non envoyée » et la pastille de compte ne lisent qu'une chose : l'existence d'une ligne `envoi_email` (`IDtype_doc = 19`, `IDreference = IDfacture`). Le seul écrivain de l'application était le `POST /:kind/:id/email`, donc une facture partie autrement restait rouge pour toujours — Pierrot complète le PDF d'Agape avec les données douane et l'envoie de sa messagerie, Isabelle groupe Lemahieu et Mod Passion dans un seul fil Gmail. Même mécanique que le rattrapage de juillet (#1020, `scripts/backfill-factures-envoyees.ts` : une ligne-marqueur par facture, reconnaissable à `notes`), exposée comme action : bouton `MailCheck` dans le trio d'en-tête (définitive rouge seulement, permission `edit_factures`) → dialogue avec un **motif obligatoire** → `POST /def/:id/marquer-envoyee` écrit UNE ligne sans adresse, `notes = "marquee envoyee hors application|<auteur>|<motif>"` (`lib/envoi-manuel.ts`, préfixe ASCII pour que le `LIKE` du retrait ne porte jamais d'accent, texte capé à 250). L'onglet Historique la rend « Envoyée hors application · Par X · motif » avec un « Annuler » (survol, même permission) → `DELETE …/marquer-envoyee` retire les lignes au préfixe ; un vrai envoi enregistré entre-temps garde la facture verte. Proforma → 404 (jamais historisé, collision d'ids). Le `GET /:kind/:id` renvoie désormais `est_envoye` comme la liste (`loadEnvoyeIds`). L'envoi groupé de factures, aussi demandé, n'est pas fait (décision Vincent 2026-09-18).

    **Clients › Commandes — adresse de livraison amputée sur la confirmation** (LIVA #1163, 2026-09-17) : l'adresse 3835 tenait sur trois lignes de rue (« Attn … », « POUR … », puis la rue elle-même en `adresse3`) et la confirmation n'en imprimait que deux — `buildLivraisonLines` poussait `adresse1`/`adresse2` là où le bloc facturation juste au-dessus poussait les trois. Même forme sur le devis ; le bon de commande sst et le bon de commande fournisseur typaient leur adresse de livraison avec UNE seule ligne de rue alors que toutes les routes passent les trois. Règle : **un bloc d'adresse imprime `adresse1`, `adresse2` ET `adresse3`**, quel que soit le document. Test `adresse-livraison.test.ts` (les quatre gabarits, adresse réelle de la 3835, parcours de l'arbre d'éléments — le `AddressCard` partagé du bon fournisseur est un composant imbriqué : le parcours invoque les éléments de type fonction, sinon il ne voit pas son texte).
## 4. Sous-traitants

**Sous-traitants** — **Commandes** (ennoblisseur; see `sous_traitants_status_model.md`), **Gestion** (`/sous-traitants/gestion`, implemented; master-detail mirror of FilsGestion over `sous_traitant`+`type_sst` with contacts/adresses — route `apps/api/src/routes/sous-traitants.ts`)

    **Fusionner des pièces à la réception ennoblisseur** (LIVA #1149, 2026-09-14). MATEL coud parfois deux pièces écru en UN rouleau teint (petites pièces → moins de coutures ; blanc pour impression) : le BL imprime `3510/11+3510/2`, un poids, un métrage, et les deux étiquettes restent sur le rouleau (l'une porte « 0 Ml »). Le modèle n'avait qu'un `stock_fini.IDstock_ecru`, et « cet écru est consommé » se lisait partout comme « un fini pointe dessus » — neuf lecteurs (stock écru, jauge et pools de Clients › Commandes, Rapports, Sous-traitants, garde de l'avis TRM, Suivi pièce, verrou du dialogue). Un fini pointant sur la seule 3510/11 aurait laissé 3510/2 « en stock chez MATEL », double-comptée, réceptionnable une deuxième fois. Solution : table de liaison **`stock_fini_source`** (une ligne par composant, première pièce incluse ; `IDstock_ecru` du fini = première pièce pour le legacy et les lecteurs existants), lib `lib/fini-sources.ts` — `consumedEcruIds()` fait l'union des deux sources, tous les lecteurs l'appellent. **UX** : dans `BatchReceptionDialog` (création seulement), Ctrl+clic ou case au survol sur une ligne de l'aperçu = fusion avec le rouleau en cours, Maj+clic = fusion de la plage (§44, l'étape courante est l'ancre) ; la fusion est immédiate (les lignes se replient en une, les compteurs « Réceptionner N » suivent), « Séparer » la défait ; la carte affiche `N pièces fusionnées : A + B · poids écru Σ` (le poids du BL doit tomber sur la somme — vérifié exact sur le BL 108968), « Couper en deux » disparaît sur un groupe ; Tricobot forme le groupe lui-même quand `num_piece` contient `+`. Un seul `POST /pieces/fini` avec `IDstock_ecru_sources` (400 si un composant n'est pas affecté à la ligne, 409 `ecru_deja_recu`, 503 `fusion_indisponible` sans la table). La carte Réception liste les composants (`source_numeros`), Finis › Stock › Provenance aussi (`composants`), Suivi pièce retrouve le rouleau depuis n'importe quelle pièce, la surteinture trace chaque composant, une coupe copie les composants sur les morceaux. ⚠️ **La table s'installe en copiant `apps/api/hfsql/stock_fini_source.{fic,ndx}` dans le dossier de la base sur le serveur HFSQL, puis en redémarrant l'API** (`CREATE TABLE` ODBC = fichier local ; une connexion liste les fichiers à l'ouverture — voir `hfsql_odbc.md` § Footguns) : `stock_fini_source (IDstock_fini_source PK entier 8, IDstock_fini entier 8, IDstock_ecru entier 8)`. Fait sur le serveur de dev le 2026-09-14, flux complet vérifié par l'API (POST groupé 201, 409 sur re-réception, pièces sorties du stock écru, Suivi pièce et Provenance, DELETE qui libère). Tant qu'elle manque, l'API le dit au démarrage, `fusion_disponible = false` cache l'affordance et tout le reste se comporte comme avant.

    **Le sélecteur de coloris d'une ligne ennoblisseur suit `ref_fini.avec_teinture`** (LIVA #1158, 2026-09-14). `GET /commandes-sous-traitant/lookups/colori-fini?ref_fini=` ne lisait que `ref_fini_colori` : pour une référence **sans teinture** (`avec_teinture = 0` — 253 des 610 finis en base, dont 329D « molleton inv moyen GR »), le catalogue est `colori_ecru` de l'écru (`ref_fini.IDref_ecru`), et la liste tombait sur « — Aucun — ». Pierre-Emmanuel a donc créé les commandes 9013 et 9015 (MATEL) sans coloris alors que « ecru/ecru » existe — l'application legacy, elle, stocke bien l'`IDcolori_ecru` dans `ligne_commande_sous_traitant.IDColoris` (1 401 lignes ennoblisseur historiques sur des finis sans teinture, 6 à zéro). La route renvoie désormais `{ id, reference, catalog }` (`catalog` = `ref_fini_colori` | `colori_ecru`, `id` = ce qui va dans `IDColoris`), même règle que `resolveColoris` du détail et que `/commandes-client/lookups/colori-fini` ; `LineFormFields` lit `id`. **Même ticket, côté document (#1157)** : le bon de commande n'affichait ni coloris (même cause) ni composition, parce que la ligne « Article initial » lisait `ref_ecru.composition`, texte libre vide sur 184 des 398 écrus (78 derrière un fini sans teinture, 329 compris). Le PDF dérive maintenant la composition de `composition_ecru` quand le texte est vide (`lib/composition-label.ts` : lignes scopées au `colori_ecru` de la ligne d'abord, puis non scopées, jamais l'union ; positions partageant un fil repliées → « 66 % 1/60 COTON PEIGNE BIO Z · 34 % 1/28 COTON PEIGNE BIO Z ») — le texte saisi garde la priorité. Garde `check-1158-coloris-fini.ts` (cde 7631 sur la copie dev) + `composition-label.test.ts`. Rattrapage prod : `IDColoris = 1551` sur les lignes 8986 (9013) et 8988 (9015).

    **Les adresses d'une commande sst se résolvent côté serveur, sur les trois chemins de création** (2026-09-14, remarque de Vincent). Trois routes insèrent un `commande_sous_traitant` : le dialogue de l'écran (`POST /commandes-sous-traitant`), l'aide « commande de tricotage » derrière Clients › Commandes, et « Ennoblir » (`commandes-client.ts`). Seule la deuxième résolvait les adresses ; « Ennoblir » écrivait `0, 0` en dur (12 commandes MATEL depuis juillet), et le dialogue faisait confiance au navigateur — qui a envoyé 0/0 sur 20 commandes MATEL entre le 30/07 et le 02/09 (signature RTF ETM sur le commentaire, donc bien le dialogue ; 8947 et 8971 en juillet puis 9013+ le 09/09 sont correctes ; code du dialogue inchangé depuis mai, ordre des routes identique sur toutes les releases, rien dans le journal API — cause non établie, ne pas y retourner). Mesure : 32 des 88 commandes MATEL depuis le 01/07 sans adresse principale, 33 sans adresse de livraison ; aucune avant juillet (897/897), aucun autre sous-traitant touché. Correctif : `lib/sst-adresses.ts` (`resolveSstAdresses` — `est_defaut` → principale, `est_defaut_livraison` → livraison, repli l'une sur l'autre puis première adresse visible ; règle du modal legacy, adresse 777 sur la commande TRM de référence) appelé par les trois inserts, le `POST` ne comblant que ce que le client a laissé à 0. **Toute nouvelle route qui insère un `commande_sous_traitant` passe par là.** Rattrapage : `scripts/repair-sst-adresses.ts` (dry-run par défaut, `--since=`, `--write`), passé sur prod le 2026-09-14.

    **Le picker « Affecter des rouleaux écru » trie par numéro et se cherche au numéro** (LIVA #1140, 2026-09-10). L'API renvoie les écru disponibles chez l'ennoblisseur en `date_saisie DESC` — des OF mélangés à l'écran (3572/14, 3572/13, 3570/10…). `LinkEcruDialog` trie désormais avec `byNumeroAsc` (numérique : 3570/8 < 3570/10) et porte une zone de recherche §5 sur `numero` + `lot`. **Filtre côté client, volontairement** : contrairement au picker des transferts (§5, `/available` plafonné à 200 → recherche SQL obligatoire), cette liste est complète (pas de TOP), donc le filtre JS est exact. Même règle #1120 que les transferts : `pruneSelection()` (`lib/transfert-picker.ts`) intersecte la sélection avec les lignes affichées à chaque changement, « Tout sélectionner » ne coche que le visible, et le sous-titre annonce « N affichés » quand une recherche est active. Vérifié sur la copie dev (cde 8836 MATEL, 31 pièces).
## 5. Transferts

**Transferts** — **Rouleaux** + **Fils** (implemented; `pages/transferts/TransfertsScreen.tsx` serves both via a `kind`, route `apps/api/src/routes/transferts.ts`). A bon de transfert (`bon_transfert` + `piece_transfert`) moves stock between magasins, and ⚠️ **the movement is immediate — `est_valide` does not gate it**: adding a piece to the bon rewrites its `IDmagasin` on the spot, removing it puts it back. The piece picker is a modal over `/available`, capped at `AVAILABLE_CAP` (200) most-recent rows per group because a magasin holds 30k+ rolls — so **its search must stay server-side**; a client-only filter would only ever search inside that arbitrary window. The search is multi-criteria (free terms AND together) with field-scoped chips via the shared `SmartSearchInput`, coloris included (that is ticket #1093 — coloris was not searchable at all, so `029 gris` and `029 ecru` were indistinguishable). Guard: `check-transfert-picker-search.ts`

    ⚠️ **La sélection du picker est toujours un sous-ensemble des lignes affichées** (LIVA #1120, corrigé 2026-09-02). Avant : l'ensemble des pièces cochées survivait à tout changement de recherche / chip / interrupteur 2e choix, le pied comptait l'ensemble entier et **Valider le postait entier** — 58 pièces cochées sous une recherche, puis 13 sous une autre : l'écran montre 13 cases, le pied dit 71, et le transfert déplace 71 pièces dont 58 que l'utilisateur ne voyait plus (le stock bouge immédiatement, §Transferts). Reproduit sur la copie dev : « Tout sélectionner » sur « 3460 » puis recherche « 3459 » → « Aucun résultat » et « 4 à ajouter ». Règle : `pruneSelection()` (`lib/transfert-picker.ts`, test Vitest) intersecte à chaque rendu la sélection de l'onglet actif avec ses lignes visibles ; les autres onglets sont intacts (leurs lignes n'ont pas changé) et le pied les **nomme** (`queuedSummary` : « 15 à ajouter (290,0 kg), dont 2 sur Fini »). Audit prod du 02/09 (`probe-1120-transferts-audit.ts`, exécuté sur l'hôte API avec son `.env`) : 24 bons depuis la mise en service du 24/07, aucun à 71 pièces — Pierrot n'a pas validé —, tous mono-référence sauf un retour MATEL de 2 finis ; aucune pièce TRM (`IDsociete = 2`) garée à un magasin ≠ 0, donc #1122 a une autre cause.
    ⚠️ **Le picker ne propose que les rouleaux d'ETM (`se.IDsociete = 1`)** (LIVA #1169, corrigé 2026-09-17). Les rouleaux que TRM vient de visiter sont eux aussi à `IDmagasin = 0` — même bâtiment, même « usine » — jusqu'à ce que TRM les expédie, moment où ils passent en société 1 (`stampShippedPieces`, `expeditions-trm.ts`). Sans filtre de société, l'usine les listait dès leur sortie du métier : le 17/09 Pierrot a mis 3568/29 à /31 (visités le matin) et 3573/23 (jamais expédié) sur le bon 4441 vers MATEL avant que TRM n'expédie, et l'avis TRM 12329 les a ensuite affichés « MATEL ». Règle : `ECRU_OWNED` dans `ecruWhere` (liste, compteurs) **et** dans la re-vérification de `PUT /:kind/:id/pieces` ; à tout magasin, pas seulement l'usine (un rouleau TRM garé à MATEL n'est pas non plus à ETM de le ramener). Le stock fini n'a pas besoin de la règle (un rouleau TRM n'a jamais d'enfant `stock_fini`). Garde : `check-transfert-picker-search.ts` (e).
    ⚠️ **« Retirer » renvoie la pièce au magasin SOURCE du bon, quel que soit l'endroit où elle est physiquement** (LIVA #1172, 2026-09-18). Trois rouleaux TRM (3568/7, 3573/13, 3574/10) transférés à MATEL le 14/09 avant leur expédition TRM (le trou #1169) ont été remis à l'usine (`IDmagasin = 0`) le 17/09 à 09:15 par le « Expédier » du **legacy WinDev** encore lancé sur PC-PIERROT (journal HFSQL `stock_ecrujnl` : User 84 = `MPS.exe`), en même temps que le tampon `IDsociete = 1` / `lot` / `IDref_commande_source`. Le picker les a donc reproposés, Pierrot les a remis sur les bons 4441/4442/4443 (doublons sur les BL), puis les a « retirés » — retour au magasin source = usine, alors qu'ils sont chez MATEL. Réparé à la main (`IDmagasin = 9` sur les trois). Décision : le legacy est désinstallé ; l'API tamponne désormais elle-même `IDref_commande_source` (`lib/trm-handover.ts`) et ne touche jamais `IDmagasin`. Rattrapage des 149 rouleaux expédiés par l'API sans ligne source : `scripts/repair-1172-source-line.ts --write` (exécuté le 18/09). Les « numéros de lot manquants » du même ticket = des pièces mises sur le bon avant l'expédition TRM (#1169) — le lot est arrivé depuis, une réimpression le montre. La pièce jointe (4 BL scannés) concorde.
    **Le picker masque le 2e choix par défaut, avec un interrupteur** (LIVA #1119 — Pierrot demandait de ne proposer que le 1er choix ; décision Vincent : masquer, pas exclure). Mesuré sur la copie dev : 1 % du tombé de métier chez MATEL mais 14 % à l'usine, et **67 des 4 907 pièces transférées en 2026 étaient de 2e choix** — rare, jamais nul, donc une exclusion dure aurait bloqué des mouvements réels. ⚠️ Le prédicat est **en SQL** (`?sc=1` sur `/available`, sinon `second_choix = 0`) : la liste est plafonnée à 200 lignes, un filtre JS ne masquerait que dans la fenêtre. La réponse porte `hidden_second_choix { ecru, fini }` (même FROM/WHERE que les loaders, `countSecondChoixHidden`) et l'écran l'affiche — « N rouleaux de 2e choix masqués — Afficher » — sinon un n° tapé qui « n'existe pas » ferait un ticket. Case « Afficher les 2e choix » dans la bande du haut, **décochée à chaque ouverture** (pas de mémoire : c'est l'exception). Les pièces déjà sur le bon viennent des lignes du bon, pas de cette requête : un 2e choix ajouté hier reste visible et retirable. Le 3e paramètre des loaders vaut `true` par défaut (les anciens appels ne changent pas). Garde : `check-transfert-picker-search.ts` §6 ; mesures : `probe-1119-second-choix.ts`. Le legacy `FEN_Gestion_d_un_bon_de_transfert` ne filtre pas le 2e choix, mais son picker fini se limite à `IDetat_stock_fini = 3` et `IDcommande_donation = 0` — pas porté, à traiter avec #1121.
    **Le picker d'écru exclut les pièces déjà teintes et masque par défaut celles affectées à un AUTRE ennoblisseur que la destination** (LIVA #1121, 2026-09-02). Trois mesures sur la copie dev commandent la règle : (1) **30 846 des 31 983 écru** que le picker offrait chez MATEL avaient un enfant `stock_fini` — teints, consommés, c'est le rouleau fini qui voyage ; ils sont exclus par le même `NOT EXISTS` que Tombé Métier › Stock. (2) Être affectée à une commande ennoblisseur est **l'état normal** d'une pièce transférée — 11 710 des 12 946 écru transférés depuis 2025 partaient VERS l'ennoblisseur de leur commande — donc l'affectation seule ne cache jamais rien : sur un aller usine → MATEL, les pièces affectées à MATEL restent visibles. (3) Le cas de Pierrot est le **retour** : pièce 3542/63 chez MATEL, affectée à la cde 8989 en attente de teinture, proposée sur un bon MATEL → usine. Règle : affectée à une commande dont `cst.IDsous_traitant ≠ IDmagasin_destination` du bon (affectation pendante = ailleurs) → masquée par défaut, comptée (`hidden_affectees.ecru`, 748 chez MATEL), case « Affectées ailleurs » + `?aff=1`, badge orange « Affectée ailleurs » quand affichée. Chaque ligne porte son affectation (« MATEL · cde 8989 »), résolue par jointure `ligne_commande_sous_traitant` → `commande_sous_traitant` dans la requête du picker (PK → PK, jamais de multiplication). Le fini reprend le prédicat du legacy `FEN_Gestion_d_un_bon_de_transfert` : `IDetat_stock_fini = 3` et hors donation. Tout en SQL (plafond de 200). Le legacy laissait passer l'écru consommé (378 des 543 retours écru MATEL → usine depuis 2025 portaient un fini). Garde : `check-transfert-picker-search.ts` §7 ; mesures : `probe-1121-affectees.ts`, `probe-1121-enfants.ts`.
    ⚠️ **Le destinataire email d'un bon de transfert se lit sur `contact.envoi_bt`, JAMAIS sur `envoi_bl`** (ticket #1097). Les deux documents ne vont pas aux mêmes personnes chez un sous-traitant, et `buildTransfertEmailDefaults()` (`routes/transferts.ts`) a réutilisé `envoi_bl` jusqu'à ce que la colonne existe. La case « Bon de transfert » se coche dans **Sous-traitants › Gestion › Contacts**, à côté de « Bon de livraison » (les cases portent le nom complet du document, pas les sigles BL/BT — demande utilisateur) — et **là seulement** : un bon de transfert part vers le magasin de destination, qui est toujours un `sous_traitant` (ou `0` = Ets Malterre, sans contact externe), donc ni la fiche client ni la fiche fournisseur n'ont besoin du drapeau. ⚠️ **La colonne est neuve et vaut 0 partout** : tant que personne n'a coché la case, un bon de transfert ne présélectionne **aucun** destinataire — les contacts restent proposés en un clic dans le dialogue d'envoi, rien n'est injoignable, ce n'est simplement plus supposé. 14 contacts chez 13 sous-traitants étaient présélectionnés par l'ancienne règle `envoi_bl` ; c'est la liste à recocher en « Bon de transfert » si l'on veut la continuité.
## 6. Fils

**Fils** (route `/fils/*`, renamed from `/fournisseurs/*`) — Références, Stock (table-centric), Commandes, Gestion, Prévisions

    **Fils › Références — les deux agrégats de la barre latérale sont des fonctions pures dans `lib/references-fil-agg.ts`** (`aggregateStockFilRows` / `resteALivrer`), parce que les deux étaient faux en production et qu'aucun ne pouvait échouer en local (ticket #1090). ⚠️ **« En commande » n'est PAS l'historique d'achat** — c'est ce qui reste attendu des fournisseurs : lignes ouvertes de commandes ouvertes, moins le reçu, exactement la règle `qte_restante` de Rapports › Commandes fils, dont l'écran doit rester le miroir. Le reçu se lit `Σ stock_fil.stock_initial` (le poids **à la réception** ; `stock` est ce qu'il en reste, ce n'est pas la même question) et se clé **sur la LIGNE, pas sur la référence** : 12 lots vivants pendent d'une ligne dont le `ref_fil` diffère. Avant correction la carte affichait le cumul de tous les achats depuis 2020, soit ×28 trop sur le catalogue. Garde : `check-references-fil-agg.ts` (test #4 = parité ligne à ligne avec le rapport) + `references-fil-agg.test.ts` pour les cas limites.
## 7. Tombé Métier

    **Tombé Métier › Stock et Fils › Stock — la recherche à critères de Finis › Stock, et le magasin Malterre affiché** (LIVA #1156, Pierrot, 2026-09-14). Pierrot voulait « une sélection multiple par référence et/ou coloris et/ou magasin » : c'est exactement ce que font les puces de `SmartSearchInput` (§27.2bis du designer), déjà en place sur Finis › Stock et Divers › Stock — les deux écrans les reçoivent au lieu d'un nouveau jeu de filtres (Tombé Métier : Référence / Coloris / Numéro / Lot / Magasin / N° Cmd / Client / Visiteur / Observations / Défauts ; Fils : Référence / Coloris / Lot interne / Lot fournisseur / Fournisseur / Emplacement / Magasin / Commentaire — le magasin n'est pas une colonne de la table fils, mais la ligne le porte). Second point du ticket : la colonne Magasin affichait « — » pour les pièces à l'usine. Mesuré sur la copie dev : **214 des 762 écru disponibles ont `IDmagasin = 0`** — le « pas de FK » HFSQL, aucune ligne `sous_traitant` derrière, donc `magasin_nom` nul. Règle : normalisé en « Malterre » dans le `select` des requêtes liste ET détail (`withDefaultMagasin`, même libellé que Finis › Stock), jamais dans la cellule, pour que la table, le tiroir, le tri et la puce `Magasin : Malterre` disent la même chose. Fils › Stock reçoit la même normalisation (`IDMagasin = 0`). ⚠️ Le harnais e2e (`apps/web/e2e`) date de juillet et ne mocke ni `/api/tickets*` ni `/api/user-profiles*` : ses specs échouent sur `unmatched` avant même la comparaison d'images — l'image de diff, elle, ne montre que le placeholder allongé et l'icône du widget tickets, la boîte de recherche et les lignes sont au pixel près.

    **Tombé Métier › Stock — audit de performance** (Vincent, 2026-09-14, dans la foulée de #1156). Trois postes mesurés : (1) **la requête** — bimodale côté serveur (~450 ms ou ~2,5 s pour le MÊME SQL, serveur inactif ; prod 0,9 s en « Disponible », 1,7 s en « Tous »), détail et règles dans `hfsql_odbc.md` § Footguns ; désormais un seul `SELECT` sans JOIN, libellés par trois lookups `IN` plats (`attachEcruLabels`) ; (2) **l'hydratation** — 620 → 230 ms en process : `fetchDefectsByEcru` en tranches de 50 (la colonne `reference` est une chaîne, une liste `IN` de 766 littéraux faisait scanner les 12 k défauts écru) et réparation des accents en un seul `repairAliased` groupé au lieu d'un `fixEncoding` ligne à ligne (32 « Démaillage ») — le helper sert 8 routes ; (3) **le navigateur** — après l'arrivée du JSON, ~700 ms de tâches longues et **1 524 éléments `[data-stock-row]` pour 762 pièces, ~34 k nœuds DOM** : la table de bureau ET la liste de cartes mobile étaient rendues, l'une cachée par `hidden md:flex` / `md:hidden`. Les quatre écrans de stock (Tombé Métier, Finis, Fils, Divers) ne montent plus qu'une branche (`useMediaQuery(MD_UP)`, `mps_designer §40.2`). Ce que l'audit n'a PAS retenu : lire le mémo `observations` dans un second `SELECT` limité aux lignes non vides (gain 150 ms en état rapide, mais **double la note en état lent** — 3,5 s contre 2,5 s en A/B entrelacé), et une pagination / virtualisation de la table (la doctrine reste « tout le stock, filtré côté client », les 762 lignes se rendent en ~200 ms une fois la branche unique). ⚠️ Toute mesure de forme SQL sur ce serveur se fait **entrelacée dans un même run** (`probe-1156-ecru-perf6/9/10.ts`), sinon la dérive du serveur invente un gagnant.

**Tombé Métier** — **Références** (implemented), rest placeholder, custom `TmRollIcon`. Références is **shared with TRM**, which imports this very file through its `@etm` alias. Its « Obs OF » tab lists `obs_ref_ecru` — the *régleur's* standing knitting notes on a reference, scoped per métier and per coloris — and here it is deliberately **read-only**: ETM buys its écru and owns no métiers, so it has nothing to write. TRM writes them from two screens, and injects its whole editable panel through the **`obsOfEditor` prop** (a component, so this file never learns a TRM endpoint; without the prop the tab is exactly what it always was). Same doctrine as `RapportFinance basePath`: a per-app difference is a prop, never a fork.
## 8. Finis

**Finis** — Références, **Stock** (implemented, table-centric; edit-mode multi-select + cut-roll via `POST stock-fini/:id/cut`), **Études coloris** (implemented), **Tarifs** (implemented) — custom `FiniRollIcon`. **Tarifs** is the price *simulator* (ports legacy `FI_Tarifs.wdw`): a `ref_tarif` row is a standalone what-if record with its own composition (`asso_fil_tarif`, whose `prix` is a per-simulation **snapshot** the user overrides — never a live read of `ref_fil.prix_kg`) and its own treatments (`asso_traitement_tarif`, same treatment may repeat). Pricing lives in `apps/api/src/lib/pricing-ref-tarif.ts`; it shares the catalog pricer's tranches/margins/majorations but with two reverse-engineered differences: the ennoblissement uplift is the manual `ref_tarif.multiplicateur`, **not** the rendement-derived MATEL multiplier, and the knitting price is the typed `prix_tricotage` rather than `ref_ecru.prix`. Dye mode is driven by `IDteinture` (0 = sans; `simple_teinture` splits simple/double, `designation_interne` splits Blanc/Tous Coloris) — `avec_teinture` on this table is a vestigial copy of the source ref. `ok_tarif` = 1 means archivée. `POST /tarifs-fini/:id/preview` prices **unsaved** parameters so the right panel recalculates live while editing (debounced). Route `apps/api/src/routes/tarifs-fini.ts`; parity check `apps/api/src/scripts/check-ref-tarif-parity.ts` (46/46 exact vs the legacy screen)

    **Finis › Stock — un rouleau d'un fini sans teinture peut porter `IDColoris = 0` s'il a été réceptionné avant le correctif #1158** (LIVA #1175, 2026-09-18) : la ligne ennoblisseur n'avait pas de coloris (le sélecteur proposait « Aucun »), la réception hérite du coloris de la ligne, et une coupe copie celui du parent — les deux rouleaux 095A (3544/1 et 3544/1-1) affichaient « — ». Réparation : `scripts/repair-1175-coloris-fini.ts` (dry run par défaut, `--write` ; prend l'`IDcolori_ecru` de la pièce écru source, sinon l'unique `colori_ecru` de l'écru ; refuse de deviner sur un fini teint ou un écru à plusieurs coloris ; répare aussi la ligne sst nourricière encore à 0). Appliqué en prod le 18/09 : 2 rouleaux, 0 ligne — aucun autre rouleau fini sans coloris depuis juillet.

    **Couper un rouleau (Finis › Stock et Tombé Métier › Stock) — le reste garde le numéro, les morceaux coupés reçoivent le suffixe suivant** (LIVA #1135, 2026-09-08). Avant : la boîte de dialogue faisait saisir la longueur coupée sur la ligne portant le numéro d'origine, et l'« auto » (le reste, sur l'étagère) recevait `-2` — Isabelle coupe 3 m d'une pièce de 5 m et trouve `3204/14-2` en stock, `3204/14` sur la commande. Règle (`lib/roll-cut.ts`, test Vitest ; `probe-1135-cut-numbering.ts` en lecture seule) : la pièce 0 envoyée à `POST /stock/{fini,ecru}/:id/cut` est le **reste** (ligne mise à jour en place, numéro conservé) ; chaque morceau saisi devient une nouvelle ligne `<base>-N` où N = **max des suffixes existants + 1** (règle MAX de `FEN_Coupe_Fini`, première coupe = `-1` comme `3204/11-1` / `3204/13-1`, `3204/13-2` en base), jamais un `-2` fixe — la seconde coupe d'un même rouleau dupliquait le numéro et couper `3204/14-2` donnait `3204/14-2-2`. `GET …/:id/cut/preview` rend `{ base, next }` pour les libellés de la boîte. ⚠️ Le pilote renvoie aussi la base nue pour `LIKE '<base>-%'` : le test « chiffres après le tiret » reste en JS.

    **Finis › Études coloris — un droit unique `edit_etudes_coloris` porte TOUS les écrits** (ticket #1092) : création / modification / suppression d'une étude et de ses soumissions, changement de statut, réponse du sous-traitant (Accepter / Refuser) et envois email. Une seule clé, un seul `ensureCanEdit()` en tête des 9 routes d'écriture de `routes/etudes-coloris.ts`, et un seul `canEdit` fileté dans `EtudesColoris.tsx` (liste, en-tête, cartes de soumission, tiroir, pilule de statut). **Les lectures restent ouvertes, les deux PDF compris** — un utilisateur en lecture seule doit pouvoir imprimer et télécharger ; sans le droit, la pilule de statut §29 perd sa moitié « Changer » et redevient un simple afficheur. ⚠️ **La clé est fermée par défaut et aucun script de rattrapage n'a été passé** (décision Vincent, 2026-08-28) : l'écran est en lecture seule pour tout le monde tant qu'un admin ne l'accorde pas dans Paramètres › Utilisateurs. Avant #1092 ce routeur n'avait **aucune garde** — `attachUser()` est best-effort et il n'y a pas de middleware d'auth global, donc les 9 routes étaient joignables anonymement ; toute nouvelle route d'écriture ici commence par `ensureCanEdit`.

    **Finis › Études coloris — « Accepté » sans soumission ne crée AUCUN coloris** (ticket #1137, 2026-09-10) : le coloris `ref_fini_colori` n'est créé que par `POST /soumissions/:id/respond` avec `accepte = 1` **et** un numéro d'échantillon (référence « <libellé>/<n°> », `IDteinture` par défaut d'après `avec_teinture`, étude pointée dessus). Passer la pilule de statut sur « Accepté » (`PUT /:id` avec `statut_col = 3`) ne fait que changer l'état : l'étude 1381 « Rose poudré 63842/2 » a été acceptée ainsi, sans soumission, et n'a donc jamais eu de coloris — la fiche 050A avait raison de ne pas l'afficher, ce n'était pas un problème de rafraîchissement (la fiche `['ref-fini', id]` se recharge de toute façon au montage, `staleTime` 0). Pierre-Emmanuel a recréé l'étude 1386 avec une soumission, et là le coloris est bien apparu… avec « poudr? » : c'est le bug `fixEncoding` sans `idField` (#1146, `hfsql_odbc.md` § Footguns). Question ouverte pour Vincent : faut-il que la pilule refuse « Accepté » sans soumission acceptée, ou crée le coloris elle-même ?
    **Finis › Références — Imprimer › Étiquette : le tag Dymo client (89 × 36) qui part avec l'échantillon** (2026-09-08). Port of the legacy `FI_Ref_Fini` « IMG_Etiquette » click (PCS-compressed window; the handler's literals are in the compile cache `MPS.cpl…FI_Ref_Fini.DAA510D5.wdw.wcw`): « Voulez-vous imprimer avec le QRCode ? », then `Réf. :` + designation, `Laize :` (`laizeHT_Moy`), `Poids/m² :` (`poids_Moy`), the pictos `lavage30.png` / `lavage40.png` (on `temp_lavage`) + `picto2..5.png` (no bleach · no tumble dry · iron · dry clean P) and a QR encoding `http://etsmalterre.fr/echantillon/?ID=<IDref_fini>`. Now `GET /references-fini/:id/etiquette` → `lib/pdf/EtiquetteRefFiniPdf.tsx` (read-only like the two other PDFs of the screen), opened straight from the §42 print menu's third row — **no options dialog: always with the QR, always one tag** (Vincent, 2026-09-08; a preview / QR-toggle / copies dialog was built and removed the same day). The QR is captioned « TARIFS ». ⚠️ **Black and white only**: a Dymo LabelWriter is a thermal printer, so the gold M badge of the first cut could not print (Vincent, 2026-09-08) — the brand is the monochrome script M (`assets/logo-m-mono.png`, every non-gold pixel of the badge → ink, trimmed and squared) painted in the centre of the QR on a white box, the QR drawn at **level H** (version 5, 37 modules) so the 30 % recovery pays for the overlay, and the type takes the width the badge freed (Réf 20 pt, designation 8 pt over three lines max, specs 12 pt, symbols 16 pt — the vertical budget is in the file's style comment). No website line, no colour anywhere. The QR keeps the **legacy URL, https'd** — the site 302s it to the live `/client/echantillon/` page and it is what every tag already in customers' hands carries; do not "shorten" it to the current path. The five care symbols moved to `lib/pdf/care-symbols.tsx`, shared with the fiche technique (same drawings, 34 pt themed there, 13 pt pure black here — a thermal head dithers anything that is not near-black); the QR is vector (`lib/pdf/QrCode.tsx`, `qrcode` matrix → one SVG path of horizontal runs, `level` prop, M by default). `temp_lavage` null/0 falls back to 30 like the fiche. ⚠️ Same unprintable right band as the écru tag (`SAFE_RIGHT = 26`): the QR sits flush against it, never past it. react-pdf 4.4 has no `maxLines`, so the designation is clamped in JS (`clampDesignation`, word boundary + ellipsis; the catalog's longest, 50 chars, fits untouched). Guard: `EtiquetteRefFiniPdf.test.ts`; visual check `scripts/dump-etiquette-ref-fini.ts <id|longest> [outDir] [rasterDir]`.
    **Finis › Références — la carte « Clients » de l'onglet Informations liste qui a déjà commandé la référence** (LIVA #1155, 2026-09-11). Port of the legacy `FI_Ref_Fini` `TABLE_Client` (one query: ETM order lines of the ref grouped by client, line count + summed quantity) — Isabelle used it as a memory aid when a reference name says nothing to her. `GET /references-fini/:id/clients` (`lib/clients-ref-fini.ts`, pure aggregator under Vitest) does three flat queries — `ligne_commande_client` with `TYPE = 2 AND IDreference`, then `commande_client` **`IDsociete = 1`** in 200-id chunks (the header set IS the partition filter: a line whose order is not returned is dropped), then `client` names by explicit columns + `fixEncoding` — and returns one row per client **sorted by volume, highest first** (Σ Ml, then Σ Kg, then name — Vincent, 2026-09-11; the first cut sorted by last order date): distinct order count, Σ Ml (`unite = 3`) and Σ Kg (anything else) kept apart, last order N° and date. Read-only, view mode only (hidden while editing like the Statistiques card), lazily loaded per reference under `['ref-fini-clients', id]`.

    **Finis › Références — la carte Spécifications est une grille de tuiles, et la référence écru vit dedans** (2026-09-11, with #1155). Vincent found the old inline « label : valeur » rows unreadable at a glance. Now: an écru row on top (`TmRollIcon` in an amber box — the Tombé Métier icon, since an écru IS a tombé de métier — reference bold + designation), then two rows of three `SpecTile`s (uppercase caption, figure at `text-2xl`; the min/moy/max tiles show the MOY big with `min · max` under it), then a thin « Stabilité & élasticité » divider over a row of four tiles (stabilité H / L, allongement H / L as min · moy · max) and a full-width « Contrôles à réception (sous-traitant) » tile that ALWAYS shows the three flags as on/off chips (gold check = on, grey circle = off — the old card hid the off ones, so « nothing shown » was ambiguous; §35 pills in edit mode) — the separate Stabilité card is gone too — then the four prose fields as full-width `TextTile`s — conditionnement, observations (production), observation technique, description commerciale — shown `whitespace-pre-line` because **every legacy conditionnement is CRLF multi-line** (one packaging fact per line; a single-line input glued them together), empty ones hidden in view mode. The former « Observations & descriptions » card is gone (Vincent: observations belong in Spécifications). Edit mode keeps the same grid with inputs in the tiles (`TileRangeInputs` = three centred cells labelled min / moy / max), Désignation above and textareas in the prose tiles; the écru `SearchableCombobox` moved here from the sidebar (default size, not `sm`). The sidebar Informations tab no longer shows the écru card; it gained a **Stock actuel** card instead (kg, Ml, lot badge, link to Stock Finis — moved out of the centre panel, Vincent 2026-09-11), so the centre panel is Spécifications › Coloris › Traitements only — **as a §39 Classeur**: a master tab row (Spécifications / Coloris / Traitements, the two catalogs carrying a plain count since the detail is already loaded, no extra fetch) gives the active dataset the full height; the three cards lost their collapsible headers, the teinture badge sits on the écru row, the coloris mode badge on a one-line caption inside its tab, and the tab resets to Spécifications on every selection change (Vincent, 2026-09-11). **Coloris tab = a table** (Coloris · Teinture [dyed only, `teinture.designation_externe`] · Note [`colori_ecru.commentaire`, column shown only when one is set] · Stock), with a GOTS leaf chip and the **live stock per coloris** (`stock_fini` GROUP BY `IDColoris`, same not-shipped predicate as the sidebar total) — the detail route batches the teinture lookup; a filter input appears above 8 coloris and resets on selection change. ⚠️ `ref_fini_colori` has NO `IDclient` column (the analysis dump ran into the next table — naming it 500ed the whole fiche on Windows, and would have been a respawn storm on Linux) and `finition` is a numeric code, not text: neither is read. **Traitements tab = the process chain**: numbered gold steps in `traitement.ordre` (now returned with `observation` by `loadRefFiniTraitements`), chevrons between them, ✕ on each step and a dashed « + Ajouter » slot at the end in edit mode. The unit is printed once, beside the figure — never also in the caption.

    **Finis › Références — les traitements se gèrent SUR la fiche, en mode édition** (ticket #1136, 2026-09-08). The Traitements card shipped read-only in the very first commit of the screen (`57bfe5c`) and nobody noticed for two months because every existing reference already carried its treatments from the legacy app — the gap only surfaced when Isabelle *created* a reference and had no way to attach « Lavage ». The junction `traitement_ref_fini` (`IDtraitement_ref_fini`, `IDref_fini`, `IDtraitement`, all ASCII) is a **strict set** — 1 570 legacy rows, zero duplicate pairs — unlike the Tarifs simulator's `asso_traitement_tarif`, which may repeat a treatment. Hence `POST /references-fini/:id/traitements` answers **409 `deja_associe`** on a second attach, and the picker only offers what is not attached yet. Writes go through `lib/traitements.ts` (`attachTraitement` / `detachTraitement`, also the shared catalog loader now used by `/tarifs-fini/lookups/traitements`); both persist immediately (no Enregistrer step, so they never dirty the §28 guard), and the card auto-unfolds when edit mode starts so the affordance is visible. Removing goes through `ConfirmDialog` for parity with the Tarifs card. The ref-delete guard (409 « possède encore des traitements ») is now reachable from the UI: detach first, then delete.

## 9. Divers

**Divers** — **Références** (`/divers/references`, implemented; `ref_divers` catalog with two variation axes + per-combination `tarif_divers` pricing — see `claude_doc/implemented_screens.md`), **Stock** (`/divers/stock`, implemented, table-centric; route `apps/api/src/routes/stock-divers.ts`)

    **Divers › Stock — `stock_divers` is SPARSE, and that drives the whole screen.** It is not a grid of every combination: it is a log of the combinations someone has touched (1 466 theoretical combos across the 505 references vs 276 rows stored — Tissu Voltige alone has 551 possible and 117 stored), and **214 of those 276 rows sit at quantité 0**. So the list **hides zero rows unconditionally** — no toggle, no filter control — leaving the ~62 lines that actually hold stock; a zeroed row is meant to be indistinguishable from a combination that was never created. A **negative** quantity is NOT hidden (data error, must stay visible). Consequence for writes: `POST /stock-divers` is an **upsert**, because 214 invisible rows would otherwise 409 "modifiez la ligne existante" for a line the user cannot find — it fills an **empty** row silently (the user's mental model is "I added a line"), but a combination that **already carries stock** still 409s and names the quantity, since silently overwriting a visible figure would destroy real data. `create_stock_divers` alone gates the upsert: it can only ever turn a 0 into a quantity. The create dialog therefore refuses quantité 0 (the line would vanish on creation), and the drawer says so when a row is zeroed. One duplicate combination exists in legacy data (ref 535 / variation 558, both rows at 0) — the upsert picks the lowest `IDstock_divers`, and refuses if *any* row of the combo carries stock.
## 10. Qualité

**Qualité** — **Suivi lots** (implemented), **Dossiers** (`/qualite/dossiers`, implemented; non-conformity dossiers over `dossier_qualite` + FNC round-trip + piece traceability — route `apps/api/src/routes/dossiers-qualite.ts`), **Actions** (`/qualite/actions`, implemented; see below), Analyse (placeholder)

    **Envoyer la FNC = a handover, not just a mail.** `POST /dossiers-qualite/:id/fnc/envoi` (and `…/fnc/email`, which does the same once the mail has actually left) stamps `envoiFNC` **and creates the `retour_client` row** that opens the dossier on the destination company's side — TRM reads it in its Qualité › Retour client screen. Until 2026-08-26 the button was an "En developpement" dialog, so a FNC sent from here reached nobody. Rules: **idempotent** (a second send never opens a duplicate dossier for the atelier); only `IDSociétéFNC = 1` (Tricotage Malterre) can be handed over (Confection's `retour_client_confection` is a different table with no screen — a FNC aimed at it still mails, and says `handover: 'unsupported_societe'`); the handover runs **after** the send, so a failed mail never opens a dossier nobody was told about. The answer comes back the other way through **`writeFncReponse()`** — the only writer of `reponseFNC` outside this file's own PUT, exported so the `"<libellé>\r\n<commentaire>"` encoding has one owner. ⚠️ `dossier_qualite.IDclient` is ETM's END client; the TRM mirror names *its* client (Ets Malterre), resolved by société name in `lib/retour-client-trm.ts` — which raises rather than guessing, since a silent fallback would file the complaint under whichever client happens to be id 1. Table primitives live in that lib (not in either route file) precisely because both routers write `retour_client`. Guard: `scripts/check-retour-client-trm.ts`.

    **Qualité › Actions** ports legacy `FI_Action_Qualité.wdw`. An *action qualité* (`action_qualite`) is a tracked quality topic owning *mentions* (`mention_qualite`) — automatic comments that print on every matching bon de commande sous-traitant — each accumulating *conformité* verdicts (`conformite_action`, one per (sst order line, mention): `Non_Contrôlé` / `Conforme` / `Non_Conforme` / `Aucun`). Data layer + matching rule: `apps/api/src/lib/actions-qualite.ts`; routes `actions-qualite.ts`; screen `QualiteActions.tsx` (Fiche). Verdicts are entered from **Suivi lots › onglet Actions**; mentions surface read-only on **Sous-traitants › Commandes** and in a red `MENTIONS QUALITÉ` block on the bon de commande PDF. Key facts:
    - **Matching rule** (validated: reproduces all 248 legacy `conformite_action` rows): a mention applies to a `ligne_commande_sous_traitant` when `lcs.type = IDtype_sst` AND `lcs.IDreference = mention.IDreference` AND (`mention.IDsous_traitant = 0` OR = the commande's sst) AND (`mention.IDColoris = 0` OR = `lcs.IDColoris`). `0` is legacy's "tous" **wildcard** here, not missing data. The mention's ref/coloris are polymorphic exactly like the line's (type 1 → `ref_ecru`/`colori_ecru`; type 2 → `ref_fini` + the `avec_teinture` coloris rule).
    - **Mentions are never persisted onto the commande** — legacy didn't either (no order `commentaire`/`journal` contains a mention's text). Resolved live so editing an action updates every future bon de commande. Single source: `GET /commandes-sous-traitant/:id/mentions-qualite`, shared by screen + PDF.
    - **`mention_qualite` has an accented PK *and* accented parent FK** (`IDmention_qualité`, `IDaction_qualité`) — no ASCII handle exists for a `WHERE`. Linux writes therefore rewrite the whole **`IDreference` bucket** (delete by the ASCII `IDreference`, positional re-insert preserving each row's PK and parent). `conformite_action`'s PK is ASCII thanks to the legacy typo **`IDconfomite_action`** (conf-**o**-mite, no `r`) — mis-reading that column name makes every PK parse as `0`, so `max+1` collapses to `1` and the next write silently overwrites real row 1. `assertPk()` guards every computed-PK site.
    - **Archiving is manual, by explicit product decision.** The per-action *objectif de conformités* (stored in `data/action-qualite-targets.json`, not HFSQL) only lights up an "objectif atteint" cue — it must never auto-close an action.
    - Guard: `apps/api/src/scripts/check-actions-qualite.ts` (`--write` for the round-trip). Its `--linux` flag **refuses to run off a Linux host**: `queryB64Text` is a passthrough on Windows, so simulating the bridge there reads mangled text and writes it back over neighbouring rows.
## 11. Rapports

**Rapports** — **Commandes clients**, **Commandes sst**, **Commandes fils** (all three implemented). Flat read-only line-level tables sharing one shape (Tableau layout, no page title, sticky sortable header, row tint red=late / amber=soon, Excel export with a per-user localStorage column picker), all served by `apps/api/src/routes/rapports.ts`. `commandes-fil` scopes like legacy `FI_Rapport_fil.wdw` (open lines of open commandes) and derives a phase per line — attente délai / en cours / réception partielle / reçue / terminée — measuring urgency against `ref_fil_commande.date_notif` while waiting for a délai and `date_livraison` afterwards. **Finance** (`/rapports/finance`) is the odd one out: it ports legacy `FI_Analyse_Finance.wdw` (Analyse › Finance) as a balance comptable rather than a line-level table, with a Charges fixes / Charges variables toggle and an N vs N-1 comparison over `upload_compta` + `compte_compta` + `releve_compta`. Amount for a year = `debit - credit` at the **last upload of that calendar year** (uploads are cumulative YTD, so never sum them, and never take January's upload which closes the prior exercise); class-7 accounts are produits and are excluded. It is the only rapport that is **permission-gated** (`view_rapport_finance`, child `edit_compte_description`) because the balance names payroll accounts. It is also the only one **shared with TRM**: the sister app imports this very file through its `@etm` alias and passes `basePath="/rapports-trm/finance"` — the frontend mirror of what `FinanceScope` is on the backend, and the ONLY per-app difference. Improve it here; never fork a TRM copy. Its routes (and the CA ones) live in **`lib/finance-common.ts`**, not in `rapports.ts` — see the router-factory bullet in §HFSQL below; the URLs are unchanged. The row drawer wears the dashboard widget header (`mps_designer §43`) with its Modifier / Annuler / Enregistrer trio in that band, and edits two fields under `edit_compte_description`: the free-text `Description` and the **fixe/variable classification** (`compte_compta.frais_variable`) — `PATCH /rapports/finance/comptes/:id` takes either field on its own, and reclassifying moves the table to the compte's new bucket (cache patched before the invalidate, else the row leaves the list for a render and the drawer blinks shut)

## 12. Réseau

**Réseau** — Entreprises
## 13. Paramètres

**Paramètres** — Utilisateurs (**admin-only**: per-user permissions + per-user email for Gmail impersonation). **TRM has its own permission catalog + store** on this API — `lib/permission-keys-trm.ts` / `lib/permissions-trm.ts` (`data/permissions-trm.json`) behind `/api/permissions-trm` — deliberately separate from `/api/permissions`: each admin screen saves by replacing the user's whole grant list filtered to its own catalog, so one shared file would let either app strip the other's grants. Gate TRM routes with `trmUserHasPermission`, never `userHasPermission`; key names may coincide with ETM's (`edit_commandes_client` exists in both). TRM likewise has its **own screen-access manifest** `lib/screen-keys-trm.ts` over its own nav tree (+ `check-screen-access-trm.ts` / `seed-screen-access-trm.ts`) — same two-direction model as ETM's, separate store. ⚠️ **The "never `userHasPermission` on a TRM route" rule breaks silently through a SHARED helper**, which is how it went unnoticed for six keys until 2026-08-27: `requirePermission()` in `lib/clients-common.ts` is imported by `clients.ts` (ETM) *and* `clients-trm.ts` / `stock-fil-trm.ts` (TRM), and it hardcoded ETM's store — so a grant made in TRM did nothing while a grant made in ETM opened the TRM route. It now takes an explicit **`PermissionScope`** (`ETM_PERMISSIONS` / `TRM_PERMISSIONS`) with **no default**, the third instance of the `FinanceScope` / `FacturesScope` shape: any guard or factory serving both companies must carry the store, and must never default it. ⚠️ **A key used by a TRM screen but absent from `TRM_PERMISSION_KEYS` fails silently and unrecoverably** — the admin tab renders no toggle (it is built from `GET /permissions-trm/keys`), the PUT drops it as unknown, `/me` never reports it, so the button is invisible to every non-admin with no way to grant it, and it reads as a deliberate restriction. Guard: **`check-permission-keys-trm.ts --web <abs path to TRM apps/web/src>`** — the mirror of `check-screen-access-trm.ts` (there the API manifest follows the web's nav; here the web's `useHasPermission` literals must all exist in the API catalog). Nothing in the type system can catch it: `requirePermission` types its key as the *union* of both catalogs, which stops a typo but not a key belonging to the other app.

**Menu / screen visibility is per user** (`claude_doc/auth_permissions.md` §Screen access). Second axis next to the action catalog, managed in the **Écrans** tab of Paramètres › Utilisateurs: a **menu is a grant, default closed** (`screen_<menu>`), a **screen inside a granted menu is a hide** (`hide_<menu>_<screen>`) — so a newly shipped screen appears for everyone who already has its menu instead of being invisible until an admin ticks it per user. A menu with no visible screen left disappears. Keys derive from the href; the API manifest `apps/api/src/lib/screen-keys.ts` mirrors `navigation.ts` and `check-screen-access.ts` diffs them. ⚠️ Hide keys are **negative** keys: read them via `hasRaw()` (no admin bypass), never `has()`, or the admin sees no menus. Enforcement is the nav surfaces + one route guard in `AppShell` — **a curtain, not a lock**: endpoints are shared across screens, so confidentiality stays with server-enforced action keys (`view_rapport_finance`, `dashboard_ca`…). Grandfathering script: `seed-screen-access.ts --write`, **run on the server** (`data/permissions.json` is gitignored and lives beside the running API), idempotent, and also the way to hand a new menu to everyone.
